from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='This project will help for recommending to uses the top 10 food items based on there recent interest and interactions with application',
    author='Team Brute Force',
    license='',
)
